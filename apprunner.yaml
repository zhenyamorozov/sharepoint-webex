version: 1.0
runtime: python311
build:
  commands:
    build:
      - yum install -y git
run:
  runtime-version: 3.11
  pre-run:
    - pip3 install --upgrade pip
    - pip3 install -r requirements.txt
  command: python3 web.py
  network:
    port: 5000
