version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  pre-run:
    - pip3 install --upgrade pip
  command: python3 web.py
  network:
    port: 5000
